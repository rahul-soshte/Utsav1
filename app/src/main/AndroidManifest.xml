<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hunter.utsav"
    android:versionCode="1"
    android:versionName="1.0">

    <application
        android:icon="@mipmap/app_icon12"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="Audition Queue"
         android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".queue"
            android:label="New Guy"

           android:noHistory="true" />
        <activity
            android:name=".PersonActivity"
            android:label="Person Details"
         android:noHistory="true"
            />

        <activity
            android:name=".AuditionsDoneList"
            android:label="Auditions Done List"
android:noHistory="true"            />

        <activity android:name=".AuditionDoneDetail"
            android:label="Person Details"
            />

    </application>

</manifest>